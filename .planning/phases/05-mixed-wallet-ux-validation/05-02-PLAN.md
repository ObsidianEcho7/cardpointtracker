---
phase: 05-mixed-wallet-ux-validation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - index.html
  - app.js
  - styles.css
files-modified:
  - index.html
  - app.js
  - styles.css
autonomous: true
requirements: [WAL-03]
user_setup: []
objective: Apply compare-first UI polish with top-4 ranking display, expandable overflow, and collapsed-by-default catalog behavior
must_haves:
    truths:
      - "The main screen reads as compare-first, with Add Card shown before the lower-priority catalog."
      - "Comparison results show only the top 4 qualifying cards by default while clearly revealing when more results exist."
      - "The catalog starts collapsed but remains easy to expand and use without changing current add/search/filter behavior."
    artifacts:
      - path: index.html
        provides: Compare-first panel ordering and catalog-toggle shell markup
        min_lines: 112
      - path: app.js
        provides: Ranking truncation, expand/collapse behavior, and collapsed catalog interaction state
        min_lines: 706
      - path: styles.css
        provides: Minimal collapsed-panel and ranking-overflow styling aligned with the existing UI
        min_lines: 417
    key_links:
      - from: index.html
        to: app.js
        via: Catalog toggle and reordered sections are wired through the existing render/event lifecycle
        pattern: "catalog|toggle|aria-expanded|panel"
      - from: app.js
        to: styles.css
        via: Ranking expansion and collapsed catalog states rely on minimal new state classes/selectors
        pattern: "collapsed|expanded|ranking"
      - from: app.js
        to: index.html
        via: Top-4 comparison rendering and expand affordance keep the callout terse while revealing extra cards on demand
        pattern: "View all|Show fewer|ranking"
---

<objective>
Ship the final compare-first polish for mixed-wallet users.

Purpose: Make the main screen easier to scan for general users without changing the underlying ranking rules validated in 05-01.
Output: Compare appears first, the catalog is collapsed by default, and the ranking list stays clean with a top-4 default plus expandable overflow.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mixed-wallet-ux-validation/05-CONTEXT.md
@.planning/phases/05-mixed-wallet-ux-validation/05-RESEARCH.md
@.planning/phases/05-mixed-wallet-ux-validation/05-01-PLAN.md
@app.js
@index.html
@styles.css
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Reorder the main screen and collapse the catalog by default</name>
  <files>index.html, app.js, styles.css</files>
  <action>Rework the existing panel order so the screen flows Compare, Your Cards, Add Card, then Catalog. Add a lightweight collapsed-by-default catalog toggle that keeps the catalog available but lower priority, preserves existing add/search/filter interactions once expanded, and stays aligned with the app's current minimalist styling.</action>
  <verify>
    <automated>node --check app.js && rg -n "Add Card|Popular Card Catalog|catalog-toggle|aria-expanded|catalog-collapsed" index.html app.js styles.css</automated>
  </verify>
  <done>The screen reads compare-first, Add Card appears before Catalog, and the catalog starts collapsed with a working toggle.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Keep the comparison panel clean with top-4 default ranking and expandable overflow</name>
  <files>app.js, styles.css</files>
  <action>Update comparison rendering so only the top 4 qualifying cards show by default, keep the best-card callout terse, and add a clear expand/collapse affordance when more results exist. Preserve existing empty and no-qualifying guidance, and do not add extra custom-vs-catalog comparison badges beyond the current UI direction.</action>
  <verify>
    <automated>node --check app.js && rg -n "View all|Show fewer|ranking-more-count|ranking-expand" app.js styles.css</automated>
  </verify>
  <done>The comparison view defaults to four results, exposes additional matches on demand, and remains easy to scan without extra explanatory clutter.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js` passes
- [ ] Compare panel is first, Add Card precedes Catalog, and Catalog starts collapsed
- [ ] Ranking defaults to the top 4 qualifying cards
- [ ] A clear expand/collapse affordance appears when more than 4 results exist
- [ ] Existing empty-wallet and no-qualifying-card guidance remains intact
</verification>

<success_criteria>
- All tasks completed
- Compare-first layout matches the locked Phase 5 context
- Ranking stays clean for larger wallets without hiding the existence of extra results
- Catalog remains discoverable while staying visually secondary
</success_criteria>

<output>
After completion, create `.planning/phases/05-mixed-wallet-ux-validation/05-02-SUMMARY.md`
</output>
