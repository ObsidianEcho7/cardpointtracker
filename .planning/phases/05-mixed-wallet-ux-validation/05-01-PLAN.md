---
phase: 05-mixed-wallet-ux-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - comparison-core.js
  - wallet-core.js
  - tests/comparison-core.test.mjs
  - tests/wallet-core.test.mjs
files-modified:
  - comparison-core.js
  - wallet-core.js
  - tests/comparison-core.test.mjs
  - tests/wallet-core.test.mjs
autonomous: true
requirements: [WAL-03]
user_setup: []
objective: Validate origin-neutral ranking and fallback behavior across empty, single-source, and mixed-wallet edge cases
must_haves:
    truths:
      - "Custom and catalog cards with equivalent rewards rank the same for the same selected category."
      - "Fallback behavior remains identical across custom-only, catalog-only, and mixed wallets."
      - "Empty, single-card, and mixed-wallet states retain deterministic comparison behavior after normalization."
    artifacts:
      - path: comparison-core.js
        provides: Source-agnostic ranking and fallback behavior for mixed wallet inputs
        min_lines: 77
      - path: wallet-core.js
        provides: Normalized wallet entities that feed comparison without origin-based drift
        min_lines: 274
      - path: tests/comparison-core.test.mjs
        provides: Regression coverage for mixed-wallet parity, fallback, and edge-case ranking states
        min_lines: 196
      - path: tests/wallet-core.test.mjs
        provides: Regression coverage for normalized custom/catalog shapes that reach comparison intact
        min_lines: 338
    key_links:
      - from: wallet-core.js
        to: comparison-core.js
        via: Comparison continues to consume normalized wallet entities without branching on origin metadata
        pattern: "normalizeWalletCards|computeComparisonResults|origin"
      - from: tests/comparison-core.test.mjs
        to: comparison-core.js
        via: Mixed-wallet fixtures lock direct-match and other-fallback parity across source types
        pattern: "category match|other fallback|catalog|custom"
      - from: tests/wallet-core.test.mjs
        to: wallet-core.js
        via: Normalization tests guarantee persisted wallet shapes stay comparison-safe before ranking
        pattern: "normalize|catalog|custom|origin"
---

<objective>
Lock mixed-wallet ranking behavior before UI polish.

Purpose: Fulfill WAL-03 and Phase 5 edge-case coverage by proving custom and catalog cards follow the same category/fallback rules.
Output: Comparison behavior is source-neutral, deterministic, and regression-protected for empty, single-source, and mixed wallets.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mixed-wallet-ux-validation/05-CONTEXT.md
@.planning/phases/05-mixed-wallet-ux-validation/05-RESEARCH.md
@.planning/phases/04-card-type-rules-editability/04-03-SUMMARY.md
@comparison-core.js
@wallet-core.js
@tests/comparison-core.test.mjs
@tests/wallet-core.test.mjs
</context>

<tasks>

## Task 1
<task type="auto" tdd="true">
  <name>Task 1: Lock comparison parity for equivalent custom and catalog reward profiles</name>
  <files>comparison-core.js, tests/comparison-core.test.mjs</files>
  <behavior>
    - Test 1: A custom card and a catalog card with the same selected-category reward rank identically except for deterministic name ordering.
    - Test 2: A custom card and a catalog card with only `other` rewards use the same fallback semantics for a non-matching category.
    - Test 3: Mixed direct-match and fallback inputs still prefer higher multipliers before tie-breaking alphabetically.
  </behavior>
  <action>Extend comparison-core regression coverage to model equivalent custom and catalog cards side by side, with explicit direct-match and `other` fallback cases. Tighten comparison-core only where needed to preserve source-neutral behavior and deterministic ordering, but do not add any origin-specific branching to the scorer.</action>
  <verify>
    <automated>node --check comparison-core.js && node --test tests/comparison-core.test.mjs</automated>
  </verify>
  <done>Comparison-core treats equivalent custom and catalog reward data the same, and regression tests lock the parity and fallback rules.</done>
</task>

## Task 2
<task type="auto" tdd="true">
  <name>Task 2: Validate normalized mixed-wallet edge cases from wallet input through ranking output</name>
  <files>wallet-core.js, tests/wallet-core.test.mjs, tests/comparison-core.test.mjs</files>
  <behavior>
    - Test 1: Catalog-only, custom-only, and mixed persisted wallet shapes all normalize into comparison-safe inputs.
    - Test 2: Empty-wallet and single-card edge cases keep deterministic comparison results after normalization.
    - Test 3: Reloaded mixed-wallet data preserves ranking parity even when catalog/custom entities originated from different storage shapes.
  </behavior>
  <action>Expand wallet-core and mixed-wallet regression fixtures so persisted custom/catalog entities continue to normalize into a comparison-safe shape for all Phase 5 edge cases. If any helper cleanup is required, keep it limited to preserving source-neutral comparison inputs rather than redesigning wallet semantics.</action>
  <verify>
    <automated>node --check wallet-core.js comparison-core.js && node --test tests/wallet-core.test.mjs tests/comparison-core.test.mjs</automated>
  </verify>
  <done>Empty, single-source, and mixed-wallet ranking inputs remain normalized, deterministic, and covered by automated regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check wallet-core.js comparison-core.js` passes
- [ ] `node --test tests/wallet-core.test.mjs tests/comparison-core.test.mjs` passes
- [ ] Equivalent custom and catalog rewards produce the same ranking semantics
- [ ] `other` fallback behavior is identical across custom-only, catalog-only, and mixed wallets
- [ ] Empty and single-card edge states remain deterministic after normalization
</verification>

<success_criteria>
- All tasks completed
- WAL-03 is protected by mixed-wallet parity regressions
- Ranking and fallback logic remain source-neutral across edge cases
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-mixed-wallet-ux-validation/05-01-SUMMARY.md`
</output>
