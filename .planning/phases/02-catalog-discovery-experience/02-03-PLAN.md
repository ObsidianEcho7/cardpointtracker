---
phase: 02-catalog-discovery-experience
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - app.js
  - index.html
  - styles.css
  - tests/catalog-core.test.mjs
files-modified:
  - app.js
  - index.html
  - styles.css
  - tests/catalog-core.test.mjs
autonomous: true
requirements: [CAT-03]
user_setup: []
objective: Add issuer filter controls and robust empty-state handling for catalog discovery
must_haves:
    truths:
      - "User can filter catalog by issuer."
      - "Issuer filter combines with name search to narrow results by intersection."
      - "When no entries match filters, UI shows explicit empty-state guidance."
    artifacts:
      - path: app.js
        provides: Combined issuer + search filtering logic with empty-state rendering
        min_lines: 460
      - path: index.html
        provides: Issuer filter control integrated into catalog section
        min_lines: 95
      - path: styles.css
        provides: Filter controls and empty-state styling for catalog panel
        min_lines: 300
    key_links:
      - from: app.js
        to: index.html
        via: issuer select and search input both drive catalog rendering
        pattern: "catalogIssuer"
      - from: app.js
        to: tests/catalog-core.test.mjs
        via: app uses tested core filtering semantics for combined filtering
        pattern: "filterCatalog"
---

<objective>
Complete catalog discovery with issuer filtering and clear zero-result behavior.

Purpose: Fulfill final Phase 2 discoverability requirement for issuer-based narrowing.
Output: Issuer filter + combined filter UX with clear no-match guidance.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-discovery-experience/02-CONTEXT.md
@app.js
@index.html
@styles.css
@catalog-core.js
@tests/catalog-core.test.mjs
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add issuer filter control and combined filtering flow</name>
  <files>app.js, index.html</files>
  <action>Add issuer dropdown control populated from catalog data and wire it into render state so search text + issuer selection are applied together.</action>
  <verify>node --check app.js && rg -n "catalogIssuer|issuer" index.html app.js</verify>
  <done>Issuer filter is interactive and combines correctly with name search.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Harden empty states and filter regression checks</name>
  <files>styles.css, app.js, tests/catalog-core.test.mjs</files>
  <action>Implement explicit no-results messaging and subtle styling for empty filter results. Extend tests to cover combined filter intersections and issuer normalization behavior.</action>
  <verify>node --test tests/catalog-core.test.mjs && node --check app.js</verify>
  <done>Empty-state handling and combined filters are clear and test-backed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/catalog-core.test.mjs` passes
- [ ] `node --check app.js` passes
- [ ] Combined issuer+search filtering and no-result state are handled explicitly
</verification>

<success_criteria>
- All tasks completed
- CAT-03 behavior implemented and discoverable
- Catalog discovery UX handles zero-match states clearly
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-discovery-experience/02-03-SUMMARY.md`
</output>
