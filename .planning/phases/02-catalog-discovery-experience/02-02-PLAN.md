---
phase: 02-catalog-discovery-experience
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app.js
  - index.html
  - styles.css
files-modified:
  - app.js
  - index.html
  - styles.css
autonomous: true
requirements: [CAT-01, CAT-02]
user_setup: []
objective: Add catalog browsing panel and name-based discovery UI
must_haves:
    truths:
      - "User can open catalog section and see built-in cards with predefined multipliers."
      - "User can search by card name and results narrow immediately."
      - "Catalog entries are presented read-only in discovery UI."
    artifacts:
      - path: index.html
        provides: Catalog discovery UI structure and controls
        min_lines: 90
      - path: app.js
        provides: Catalog state wiring and name-search rendering flow
        min_lines: 420
      - path: styles.css
        provides: Catalog list/read-only card presentation styles
        min_lines: 280
    key_links:
      - from: app.js
        to: catalog-core.js
        via: app bootstrap loads and uses catalog helper module
        pattern: "catalogCore"
      - from: app.js
        to: index.html
        via: catalog nodes and search input drive rendered list
        pattern: "catalog"
---

<objective>
Deliver the first user-visible catalog discovery flow with name search.

Purpose: Let users browse built-in popular cards and quickly narrow by name before wallet integration.
Output: Catalog panel rendered in app with real-time name-based filtering.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-discovery-experience/02-CONTEXT.md
@app.js
@index.html
@styles.css
@catalog-core.js
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Add catalog panel markup and base styles</name>
  <files>index.html, styles.css</files>
  <action>Add a new catalog section with heading, search input, count text, and read-only result list container. Style entries for readability and responsive scanning while preserving existing visual language.</action>
  <verify>rg -n "catalog" index.html styles.css</verify>
  <done>Catalog section is visible and styled for read-only browsing.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Wire catalog rendering and name search</name>
  <files>app.js</files>
  <action>Load catalog core during bootstrap, hydrate catalog state from fixed dataset, render catalog rows, and filter results using case-insensitive card-name search as user types.</action>
  <verify>node --check app.js</verify>
  <done>Catalog list renders in app and name search narrows results immediately.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check app.js` passes
- [ ] `node --test tests/catalog-core.test.mjs` still passes
- [ ] Search input updates catalog list without page reload
</verification>

<success_criteria>
- All tasks completed
- CAT-01 and CAT-02 behaviors are available in UI
- Discovery flow remains read-only in this phase
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-discovery-experience/02-02-SUMMARY.md`
</output>
