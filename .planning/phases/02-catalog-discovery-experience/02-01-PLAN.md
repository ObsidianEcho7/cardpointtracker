---
phase: 02-catalog-discovery-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - catalog-core.js
  - tests/catalog-core.test.mjs
files-modified:
  - catalog-core.js
  - tests/catalog-core.test.mjs
autonomous: true
requirements: [CAT-01]
user_setup: []
objective: Add fixed catalog dataset and deterministic normalization/filter helpers
must_haves:
    truths:
      - "System includes a built-in fixed catalog of popular cards with predefined multipliers."
      - "Catalog normalization returns consistent card shape (id, name, issuer, rewards[]) with valid multipliers."
      - "Catalog helper logic is reusable in browser runtime and Node test environment."
    artifacts:
      - path: catalog-core.js
        provides: Catalog seed data, normalization, issuer extraction, and filtering helpers
        min_lines: 120
      - path: tests/catalog-core.test.mjs
        provides: Regression tests for normalization and filter behavior
        min_lines: 80
    key_links:
      - from: tests/catalog-core.test.mjs
        to: catalog-core.js
        via: Node tests import and validate catalog helper behavior
        pattern: "catalog-core"
---

<objective>
Create a shared static catalog core module that defines card seed data and normalized helper functions.

Purpose: Establish reliable catalog data and deterministic filtering behavior before UI wiring.
Output: Reusable catalog module plus automated regression checks.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-discovery-experience/02-CONTEXT.md
</context>

<tasks>

## Task 1
<task type="auto">
  <name>Task 1: Create shared catalog core module</name>
  <files>catalog-core.js</files>
  <action>Implement a pure shared module with a fixed card seed list and helper functions for normalization, issuer extraction, and combined search/filtering. Normalize card ids and reward entries so downstream UI can render without additional data cleanup.</action>
  <verify>node --check catalog-core.js</verify>
  <done>`catalog-core.js` exports deterministic catalog helpers with browser + Node compatibility.</done>
</task>

## Task 2
<task type="auto">
  <name>Task 2: Add regression tests for catalog normalization and filtering</name>
  <files>tests/catalog-core.test.mjs</files>
  <action>Create Node built-in tests that validate normalized card shape, stable issuer extraction, case-insensitive name search, issuer filter behavior, and combined search+issuer narrowing.</action>
  <verify>node --test tests/catalog-core.test.mjs</verify>
  <done>Automated tests protect catalog behavior from regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check catalog-core.js` passes
- [ ] `node --test tests/catalog-core.test.mjs` passes
- [ ] Catalog helper outputs are deterministic for the same input
</verification>

<success_criteria>
- All tasks completed
- Fixed built-in catalog dataset established
- Normalization/filtering behavior is test-covered and deterministic
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-discovery-experience/02-01-SUMMARY.md`
</output>
